# Stepper forms

import Stepper from "@/components/grouped-forms/Stepper";
import schema from "@/components/grouped-forms/Stepper/schema.json";
import SchemaViewer from "@/components/SchemaViewer";

<Stepper />

## Schema:

<SchemaViewer value={schema} />

## Usage:

```tsx
import React from "react";
import schema from "<your_schema.json>";
import MuiForms from "mui-forms";

function MyWizard() {
    return (
        <MuiForms
            sectionLayout="stepper"
            config={{
                variant: "filled"
            }}
            schema={schema}
            onSubmit={() => {
                // do nothing
            }}
        />
    );
}
```
