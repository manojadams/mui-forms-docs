# Wizard forms

import Wizard from "@/components/grouped-forms/Wizard";
import SchemaViewer from "@/components/SchemaViewer";
import schema from "@/components/grouped-forms/Wizard/schema.json";

<Wizard />

## Schema:

<SchemaViewer value={schema} />

## Usage:

```tsx
import React from "react";
import schema from "<your_schema.json>";
import MuiForms from "mui-forms";

function MyWizard() {
    return (
        <MuiForms
            sectionLayout="wizard"
            config={{
                variant: "filled"
            }}
            schema={schema}
            onSubmit={() => {
                // do nothing
            }}
        />
    );
}
```
