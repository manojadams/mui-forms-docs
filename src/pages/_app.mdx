import "../styles.css";
import { Analytics } from "@vercel/analytics/react";
import lato from "../components/font";
import "@manojadams/nextra-theme-docs/style.css";

export default function App({ Component, pageProps }) {
    return (
        <main className={lato.className}>
            <Component {...pageProps} />
            <Analytics />
        </main>
    );
}
